# logsentry
A command-line server log analyzer built with Go that processes web server logs, extracts traffic insights, detects HTTP errors, ranks the most active client IPs, and generates audit reports.
